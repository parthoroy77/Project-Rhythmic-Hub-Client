import { useQuery } from '@tanstack/react-query';
import React from 'react';

const MyClass = () => {
    const {} = useQuery()
    return (
        <div>
            
        </div>
    );
};

export default MyClass;